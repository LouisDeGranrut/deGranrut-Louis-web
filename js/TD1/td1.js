'use strict'

function min(a,b) {
  return (a<b) ? a : b;
}

let max(a,b){
    return (a>b) ? a : b;
}

console.log("Min entre 224 et 42 : " + min(224,42);
console.log("Max entre 0.1 et 0.009 : " + min(0.1,0.009);
